<br>
<br>
<h4><%= link_to "^", link_path(@link, "@link[vote]" => (@link.up_vote)), :data => {:method => :patch} %>
    <a href="<%= @link.user_link %>" target="_blank"><%= @link.title %></a></h4>
    <h6>votes: <%= @link.vote %> <%= @link.long_ago %> <a href="/links/<%= @link.id %>">comment</a></h6>

<%= form_for(@comment) do |f| %>
  <div class="form-goup">
    <%= f.label :Comment %>
    <%= f.text_field :comment %>
  </div>
  <br>

<%= f.submit(:class => "btn btn-primary") %>

<% end %>
